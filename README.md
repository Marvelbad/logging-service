# 🚀 Logging Service (Java + Logback)

Мини-проект, демонстрирующий грамотную настройку системы логирования в Java  
с использованием **SLF4J**, **Logback**, раздельных лог-файлов,  
**ротации логов (RollingFileAppender)** и **MDC (requestId)**.

Проект построен в стиле реальных микросервисных систем.

---

## 🧰 Стек технологий
- **Java 22**
- **Maven**
- **SLF4J**
- **Logback**

---

## 🔥 Реализованный функционал

### ✔ Базовая конфигурация логирования
- Консольный логгер  
- Файловый логгер (`app.log`)

### ✔ Отдельный лог-файл ошибок
- Выделенный `error.log`  
- Добавлен через **LevelFilter**, принимает только ERROR-записи

### ✔ Ротация логов (RollingFileAppender)
- Ежедневная ротация логов  
- Автоматическое архивирование (`.log.gz`)  
- `maxHistory` для ограничения количества архивов  

### ✔ MDC (Mapped Diagnostic Context)
Реализовано добавление **requestId**:

- В начале выполнения создаётся UUID  
- Встраивается в каждый лог в рамках одного "запроса"  
- Позволяет легко отслеживать цепочку логов

Пример:

### ✔ Пример бизнес-логики (сервисный слой)
`UserService` демонстрирует:

- `createUser` — INFO  
- `deleteUser` — WARN  
- `failUser` — ERROR с исключением

---

## 📂 Структура проекта

src/main/java/com/badri/logging/
App.java
service/
UserService.java
